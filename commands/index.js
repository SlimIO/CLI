const initAgent = require("./init");
const addAddon = require("./addAddon");
const create = require("./create");
const connectAgent = require("./connect");

module.exports = {
    initAgent,
    addAddon,
    create,
    connectAgent
};
